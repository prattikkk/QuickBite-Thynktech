# Capacitor-specific environment variables.
# Used when building for native mobile (Android / iOS).
#
# Usage:
#   cp .env.capacitor .env.local   # .env.local overrides .env
#   npm run build
#   npx cap sync

# Backend API — absolute URL required for native WebView
VITE_API_BASE_URL=https://api.quickbite.com/api
VITE_NATIVE_API_URL=https://api.quickbite.com/api

# WebSocket — absolute wss:// URL
VITE_WS_URL=wss://api.quickbite.com/ws
VITE_NATIVE_WS_URL=wss://api.quickbite.com/ws

# Signal that this is a Capacitor build (disables PWA service worker)
VITE_CAPACITOR=true

# Stripe
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_51T2mblCPN82wj1A88QTXuX6025HXeI32XregpcdflAdoCyEZlAQ5pFSFA1tbCvKeSvs8RQLN8Fdzgdppyrbp4gEv00y1Bqw9u3

# WebSocket toggle
VITE_USE_WEBSOCKET=false
VITE_POLL_INTERVAL=5000
